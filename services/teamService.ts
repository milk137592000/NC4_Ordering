// This service is no longer used after switching to LINE Login.
// Team members are now dynamically determined by who logs in on a given day.
// Keeping the file for reference, but its functions are deprecated.

// import { collection, getDocs } from 'firebase/firestore';
// import { db } from '../firebase';
// import type { User } from '../types';

/**
 * @deprecated Fetches the list of team members from the Firestore 'teamMembers' collection.
 */
// export const getTeamMembers = async (): Promise<User[]> => [];
