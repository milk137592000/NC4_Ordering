// This service is no longer used after switching to LINE Login.
// Team members are no longer pre-seeded as users are identified via their LINE profile.
// Keeping the file for reference, but its functions are deprecated.

// import { collection, getDocs, writeBatch, doc, limit, query } from 'firebase/firestore';
// import { db } from '../firebase';
// import type { User } from '../types';

/**
 * @deprecated Checks if the 'teamMembers' collection is empty and, if so, populates it
 * with a predefined list of members.
 */
// export const seedTeamMembersIfNeeded = async (): Promise<void> => {};
